#include <stdio.h>

int main()
{
    int length;
    int breadth;

    printf("what is the length of rectangle %d\n",length);
    scanf("%d", &length);
    printf("what is the breadth of reactangle%d\n",breadth);
    scanf("%d", &breadth);
    printf("the area of rectangle is %d", length * breadth);

    return 0;
}